import { PinataConfig } from '@pinata/sdk';
import { PinataOptions } from '@pinata/sdk/types/commands/pinning/pinFileToIPFS';
import { PinataMetadata } from '@pinata/sdk/types/commands/data/pinList/pinList';
export interface PinataPinByHashPinOptions {
    pinataMetadata?: PinataMetadata;
    pinataOptions?: PinataOptions;
}
export interface PinataPinByHashResponse {
    id: number | string;
    ipfsHash: string;
    status: string;
    name: string;
}
export default function pinByHash(config: PinataConfig, hashToPin: string, options: any): Promise<any>;
